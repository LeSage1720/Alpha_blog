<div class="text-center jumbotron jumbotron_home">
  <div class="container show-case">
    <h1>Hola, Bienvenidos!</h1>
     <div class="container">
      <p class="text-center">
        
          ProHacktividad es una Aplicación Web para compartir 
          conocimientos, artículos, archivos, libros PDF relacionados a la programación.
          <br>
          <hr>
        <h4 style="color:cyan;">WebApp Under Construction</h4>
        <em>Programmer:<b>Paterson Anaccius</b> con la ayuda de <b>Don Juan</b></em>
      </p>
     </div>
  </div>
  <!--<p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a></p>-->
  <%= link_to "Pater Blog", articles_path, class: "btn btn-primary btn-lg" %>
  <%= link_to "Acerca", about_path, class: "btn btn-primary btn-lg" %>
</div>


<!-- SHOW RECENT POST ON HOME PAGE-->
<div class="section-1-header">
    <hr>
    <h2 class="text-center">Publicaciones recientes</h2>
    <hr>
</div>

    <div class="row"> 
        <% @article.last(6).each do |a| %>
             
            <div class="col-6 col-sm-6 col-lg-6">
                <h2><%= link_to a.title, article_path(a.id)%></h2>
                <em><%= a.created_at%></em>
                <p><%= sanitize a.description[0...150] + "..."%></p>
                <%= link_to "Leer más»", article_path(a.id), class: "btn btn-default" %>
                 <% if logged_in? && (current_user == a.user || current_user.admin?) %>
                   <%= link_to "Edit", edit_article_path(a.id), class: "btn btn-default" %> |
                   <%= link_to "Delete", article_path(a.id), class: "btn btn-danger", method: :delete, data: {confirm: "Are you sure"} %>
                 <%end%>
                <hr>
            </div>
        <% end %>
    </div> 
    



<div class="section-2-header">
  <hr>
  <h2 class="text-center">Plantillas recientes</h2>
  <hr>
</div>